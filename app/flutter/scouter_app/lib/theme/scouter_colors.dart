import 'package:flutter/material.dart';

class ScouterColors {
  ScouterColors._();

  // Background
  static const background = Color(0xFF1A1A2E);
  static const surface = Color(0xFF16213E);
  static const surfaceLight = Color(0xFF222244);

  // Function colors
  static const red = Color(0xFFFF4444);        // Cancel / Delete / Destructive
  static const blue = Color(0xFF4488FF);       // Home / Navigation anchor
  static const yellow = Color(0xFFFFDD44);     // Scan QR / Numpad accent
  static const green = Color(0xFF44FF44);      // Confirm / Send / OK / Enter
  static const orange = Color(0xFFFF8844);     // URL Input / Secondary nav
  static const purple = Color(0xFFBB66FF);     // AI Assistant
  static const cyan = Color(0xFF44DDFF);       // Keyboard / Alpha accent
  static const gray = Color(0xFF666688);       // Inactive / Hints

  // Text
  static const textPrimary = Color(0xFFEEEEFF);
  static const textDim = Color(0xFF8888AA);

  // Borders
  static const border = Color(0xFF333355);
}
